# SkyN3t — Mission

A self-healing, self-learning, autonomous multi-agent system that talks to multiple LLMs, has conversations between agents to prevent hallucinations, and builds real, runnable programs. Learn from all the different githubs out there in building apps , ai agents, llm ,rags ,memory, mistakes and build a massive smart program that learns creates autonomously. 

## What we are building toward

**A program that takes a brief and produces a real, working program — not a markdown summary, not a fake demo. Prompt if need more information to make a perfect Web/Web Apps, Mobile app, Computer App, etc, builds the packaging at the end as well or at least a script to make it easy to install them - not sure whats best**


## The  non-negotiables

Quality Products, Fast, Better than Hermes, OpenClaw, Paper Clip all differnt githubs
### 1. Multi-LLM, if possible 

Install  has a wizard that allows you to install Different  CLI agents on different backends  as well using api like openrouter and whatever the other ones are — example Designer on Opus, CodeAgent on Codex, Research on Copilot, Reviewer on Sonnet. No agent should be locked to a model. Switching backends is a config edit, not a code change. also add a auto mode that uses the  best ai model for the job also option to bring into other intgrations as well replicate or some of the big major ones for building the apps, like photos, creating ui design, game sprites etc, also know all different types of best practiced for things.

### 2. Agents have conversations

Linear handoff is last resort . Designer proposes → Reviewer critiques → Designer revises. CodeAgent writes → Reviewer pushes back → CodeAgent fixes. **Cross-model debate** — one LLM proposes, a different LLM critiques. This is the anti-hallucination layer. We will need help designing what is best in case that order isnt best im sure depending on app or what a person build as well


### 3. Real memory, no amnesia

Past projects, past failures, past lessons all stay queryable. Skills library, build pattern scoreboard, RAG, Cortex history, lesson attribution. The system should remember "I tried this last week and it didn't work" without being told. be a brain learn from mistakes, self heal as coding, comunicate between agents if we can help the out put. Not sure if this best but we can get ides fro GitHub example https://github.com/garrytan/gbrain

### 4. Self-healing

Failed builds become fix loops. Failure patterns become Cortex proposals. The system doesn't need a human to notice it's been failing. auto import  user doesnt have to do anything just learns from mistakes
### 5. Self-learning

Every successful build teaches the system. Skills library captures what worked. Build pattern scoreboard remembers which file shapes succeed per stack. The system gets better with each project, not just per-conversation.

auto import - learn about top github for the day week month. 
## What we are NOT

- A markdown generator
- A demo builder
- A chat wrapper
- A one-shot prompt → code tool
- A clone of any existing thing - we do get ideas from other githubs

We compete with Hermes Agent, OpenClaw, Paperclip. We match their features (skills, ACP, isolated subagents, Docker backend, persistent memory, messaging adapters) and add ours (inter-agent conversation, multi-LLM debate, brutal verification, real autonomy).

## Subscription-based — or API

Backends are subscription-billed CLIs (claude_cli, copilot_cli, kimi_cli) and direct API. we always checks sites like openrouter to look at new models  to figure out what is best ofr a project or cheapest way or free way 

## Interface
- LLM CLI - to communicate back and forth to run builds to watch agents work to see progress  - as well as a UI to see the cortex work like a brain showing what agents is working in a beautiful visualization highlight while working as well able ro build projects from the ui as well as other useful features like configuration - adding apis  etc

Initial install
 Wizard - add your ai  platforms, CLI and API ,  like calude clode cli , kimi etc api like openrouter etc
 intregrations - Discord, telegram, slack , sms , UI design apps like penpot or others  like WeCom, Lark, Line, KakaoTalk, Hermes has a list 

Agents -  will be created as needed  and saved as different projects are built - agents can be configured to use different ai models or cli  by default it uses the best free ai models - other options are auto cheapest models and auto best overall  prob need a price thing to handle it 
subagents can be used if it will help or open to any other ideas or best ideas 

Real autonomy loops

## Docker subagent runners by default

## Web search backend for ResearchAgent
## iOS / Swift / mobile builds
Code audit

if i missed anythngi add it 
add skills same as hermes but more